import React from 'react'

export default function App() {
  return(
    <div style={{ padding: 'var(--space-12)' }}>
      <h1 style={{ color: 'var(--color-primary)'}}>HKIF is working ✓</h1>
      <p>Vite + React is set up correctly.</p>
    </div>
  )
}